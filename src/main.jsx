import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import EstacionComponent from "./EstacionComponent";
ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EstacionComponent />
  </React.StrictMode>,
);
